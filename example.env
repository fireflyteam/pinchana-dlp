# Example Environment Variables for Pinchana DLP

# Redis password for securing the internal database
REDIS_PASSWORD=YourSuperSecurePasswordHere123!

# Internal token used for API <-> Worker authentication.
# Generate a strong random string (e.g., openssl rand -hex 32)
INTERNAL_TOKEN=change_me_to_a_random_secret_string

# The absolute path on the host machine where data should be stored.
# Change this if you want to store jobs outside the current directory.
HOST_DATA_DIR=./data

# If you are hosting the API behind a reverse proxy (like Nginx/Traefik)
# on a subpath like example.com/tool, set the API_ROOT_PATH here.
# API_ROOT_PATH=/tool


# Local IP and Port to bind the API to
API_BIND_HOST=127.0.0.1
API_PORT=8080

# NordVPN VPN Rotation (Optional)
# NORDVPN_USER=your_nordvpn_username
# NORDVPN_PASS=your_nordvpn_password
# NORDVPN_COUNTRY=US
# PROXY_URL=http://vpn:8888
